# -*- mode: ruby -*-

ignore "vendor/{jquery,jquery.print,jquery-ui,strophejs,dramatis}/**"

web = "{public,lib}/**/*"

haml web
sass web
jslintrb web

testflock :options => {
  :all => [ "envjs:", "xmpp:checkmate@testflock" ],
  :url => "http://0.0.0.0:8000"
}

jazz web, :options => {
  :driver => [:envjs, :testflock]
}
