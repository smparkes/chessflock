# -*- mode: ruby -*-

ignore "vendor/{jquery,jquery.print,jquery-ui,strophejs,dramatis}/**"

web = "{public,lib,spec,vendor/{feste,puck}/{lib,spec}}/**/*"

haml web
sass web
jslint_rb web

if false
  testflock :options => {
    :all => [ "envjs:", "xmpp:chessflock@chessflock" ],
    :url => "http://0.0.0.0:8000"
  }
end

jazz web, :options => {
  :driver => [:envjs, :testflock]
}
